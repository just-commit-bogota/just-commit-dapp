import React from "react"

export default function UploadButton(props) {
    function todo() {

    }
    return (
        <button className="card__button" onClick={props.onClick}>Upload</button>
    )
}